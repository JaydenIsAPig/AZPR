# V7 Finding-Closure Matrix for v8

| Finding | Status | Affected paths | Implemented invariant | Tests / observed result | Remaining limitation |
|---|---|---|---|---|---|
| AZPR-V7-F01 | `CLOSED_AND_TESTED` | `trusted-installation/install.py`; qualification authorization schemas; authorization creation tool | A fresh, independently dual-signed, exact one-use authorization is verified read-only before recovery, key access, directory creation, or state writes. Its reservation is the first permitted write. | `test_installer_authorization_is_first_gate_and_policy_phase_is_explicit`; `blackbox.v8.installer_authorization_before_state`: PASS. | Real independent signers and a dedicated host were not available. |
| AZPR-V7-F02 | `CLOSED_AND_TESTED` | `operator-tools/verify_v8_cleanroom_prerequisites.py`; 7 new/updated schemas; fixture builder | Exact 39-artifact set; strict schemas; role-separated signatures; freshness/revocation; executable probes; SBOM/provenance/image/registry/report/raw-evidence cross-bindings. | Positive semantic fixture PASS; unsigned mutated report rejected; empty fabricated corpus rejected; `blackbox.v8.fabricated_qualification_rejected`: PASS. | Synthetic positive evidence proves verifier behavior, not real host truth. |
| AZPR-V7-F03 | `CLOSED_AND_TESTED` | both `secure_runtime.py`; both `process_gate.py`; controller result lifecycle | Target cannot start before cgroup attachment. Trusted root gate enters PID/mount/network namespaces, forks namespace init, drops groups/GID/UID and no-new-privileges. Controller terminates and proves the full cgroup empty before result inspection. | Detached descendant black-box PASS; output-overflow teardown PASS; inherited process-tree tests PASS. | Real cgroup/namespaces/LSM and exact Codex require host qualification. |
| AZPR-V7-F04 | `CLOSED_AND_TESTED` | both `secure_runtime.py` | stdin, stdout, and stderr enter one nonblocking selector immediately; one monotonic deadline covers transfer, execution, teardown, and drain. | Bidirectional 2 MiB saturation PASS; early exit/broken pipe and timeout tests PASS; overflow teardown PASS. | Real Codex/adapter throughput must be characterized. |
| AZPR-V7-F05 | `CLOSED_AND_TESTED` | `secure_runtime.py`; installer; quota schema/profile | Production run surface is an exact tmpfs with byte/inode limits and `nodev,nosuid,noexec`; scan enforces entries, allocated blocks, file size, depth, path length, file types, and cleanup deadline. Surface is sealed before result import. | Quota scan size/FIFO tests PASS; mandatory source markers and profile cross-bindings PASS. | Root mount, millions-of-files, disk/inode exhaustion, concurrent cleanup, and reboot tests remain host gates. |
| AZPR-V7-F06 | `PARTIALLY_CLOSED` | key lifecycle schemas/tools | Dual-role signatures, exact current epoch/fingerprint, create-once transition receipt, remote-anchor acceptance, transition chain, historical verification metadata, replay and rollback rejection. | Rotation and replay test PASS. | Not yet applied atomically to real installed controller/evidence/approval/adapter key stores; remote anchor is synthetic. |
| AZPR-V7-F07 | `CLOSED_AND_TESTED` | reports ZIP, delivery manifest, verification records | One self-contained delivery manifest resolves every top-level artifact; scoped manifests verify both ZIPs; reports state the actual DOCX result from independent consultation. | Fresh-extraction and reports verification required before release. | No organization signing private key was supplied; release remains explicitly unsigned-development. |
