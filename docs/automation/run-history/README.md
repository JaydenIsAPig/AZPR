# Run History

The controller writes one committed Markdown record here for each successfully prepared stage branch. Runtime transcripts and raw structured results remain local under `.codex-loop/` and are not committed.
